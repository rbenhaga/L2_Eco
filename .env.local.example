# Firebase Configuration
# Replace these with your actual Firebase project credentials from the Google Cloud Console

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE _FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com  
VITE_FIREBASE_MESSAGING_SENDER_ID=your_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# Instructions:
# 1. Create a Firebase project at https://console.firebase.google.com
# 2. Enable Google Authentication in Firebase Console > Authentication > Sign-in method
# 3. Copy your Firebase project configuration
# 4. Replace the values above with your actual credentials
# 5. NEVER commit this file to Git (it's already in .gitignore)
